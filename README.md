# Go + React Landing Page

Simple landing page using:
- Go (`net/http`) for the web server
- React for UI rendering in the browser

## Run

```powershell
go run .
```

Open: http://localhost:8080"# goreact" 
